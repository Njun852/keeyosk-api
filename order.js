class Order {
    constructor(tblId, time, customerId, status, cart, quantity) {
        this.tblId = tblId
        this.time = time
        this.customerId = customerId
        this.status = status
        this.cart = cart
        this.quantity = quantity
    }
}